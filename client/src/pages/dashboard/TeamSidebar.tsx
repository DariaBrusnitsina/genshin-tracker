import React from 'react'
// import { Avatar, List, ListItemButton, Stack } from '@mui/material'

const TeamSidebar: React.FC<string> = () => {
  return (
    <p>hello</p>
  // <List>
  //   {teams.map((t, i) => { return (

  //   <ListItemButton
  //     selected={selected === i}
  //      onClick={(event) => onClick(event, i)}
  //   >
  //     <Stack direction="row" spacing={0.5} marginY={1} >
  //       {t.map((ch) => {return <Avatar>{ch.char.name[0]}</Avatar>})}
  //     </Stack>
  //   </ListItemButton>
  //   )})}
  // </List>
  )
}

export default TeamSidebar
